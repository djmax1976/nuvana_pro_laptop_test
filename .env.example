# Frontend Environment Variables Template
# Copy this file to .env.local and update with your values
# NEVER commit actual secrets or production URLs to source control
#
# @see OPS-012: CONFIG_SECURITY - Environment-specific configs
# @see SEC-016: SECRET_MANAGEMENT - Never commit secrets to source control

# =============================================================================
# API Configuration
# =============================================================================
# Backend API URL - Used for all API calls from the frontend
# Development: http://localhost:3001
# Production: Set via deployment platform environment variables
#
# IMPORTANT: This is a NEXT_PUBLIC_ variable, meaning it's exposed to the browser.
# Only use this for the public API endpoint, never for internal services.
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Legacy API URL variable (deprecated - use NEXT_PUBLIC_BACKEND_URL)
# Kept for backwards compatibility with existing middleware
NEXT_PUBLIC_API_URL=http://localhost:3001

# =============================================================================
# Database Configuration (if using Prisma from Next.js API routes)
# =============================================================================
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Development uses local PostgreSQL; production uses managed database service
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nuvana_dev

# =============================================================================
# Feature Flags (optional)
# =============================================================================
# NEXT_PUBLIC_ENABLE_DEBUG_MODE=false

# =============================================================================
# Analytics (optional)
# =============================================================================
# NEXT_PUBLIC_ANALYTICS_ID=

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# For production deployments (Vercel, Railway, AWS, etc.):
#
# 1. Set environment variables in your deployment platform's dashboard
# 2. NEVER copy production values into this file
# 3. Required production variables:
#    - NEXT_PUBLIC_BACKEND_URL: Your production API URL (e.g., https://api.nuvanaapp.com)
#    - DATABASE_URL: Your production database connection string (via secrets manager)
#
# For local development:
# 1. Copy this file: cp .env.example .env.local
# 2. Update values for your local environment
# 3. .env.local is gitignored and safe for local secrets
