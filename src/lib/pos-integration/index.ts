/**
 * POS Integration Library
 *
 * Re-exports all POS integration utilities for convenient imports.
 *
 * @example
 * ```typescript
 * import {
 *   POS_TYPE_CONFIGS,
 *   getPOSTypeConfig,
 *   isFileBased,
 *   formatSyncInterval,
 * } from "@/lib/pos-integration";
 * ```
 *
 * @module lib/pos-integration
 */

export * from "./pos-types";
